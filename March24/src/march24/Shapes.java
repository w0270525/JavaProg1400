package march24;

public abstract class Shapes {
/* timer for drawing frames
 at least 4 moving shapes
     one irregular 
     class for each object
    must bounce off walls
    shapes should conform to size of the window
    additional shapes added at runtime
    collision detection
    action listener= button click, changing focus
    
    commented, indented properly, 
    4 unique visual state changes (color, gradient, pattern)
    10% for the shape (graphics 2d objects) 
    able to add shapes while running
    */
    
	
}
